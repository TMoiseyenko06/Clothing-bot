version: '3.8'

services:
  app:
    build: .
    ports:
      - "${APP_PORT:-80}:80"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - SERPER_API_KEY=${SERPER_API_KEY}
      - OUTFITS_DIR=/outfits
    volumes:
      - outfits_data:/outfits

volumes:
  outfits_data:
